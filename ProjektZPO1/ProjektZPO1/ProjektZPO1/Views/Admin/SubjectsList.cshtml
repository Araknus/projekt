@model IEnumerable<ProjektZPO1.Models.Subject>
@{
    ViewBag.Title = "SubjectsList";

    if (Session["Id"] == null || Session["UserType"].ToString() != "2")
    {
        Response.Redirect("~/Home/Index");
    }
}

<div class="jumbotron">
    <h1>
        Lista przedmiotów
    </h1>
    <div class="text-right">
        <button class="btn btn-primary btn-lg" onclick="location.href='@Url.Action("Index", "Admin")'">
            Cofnij
        </button>
    </div>
</div>

<table class="table table-condensed">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClassRoom.Name)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ClassRoom.Name)
                </td>
                <td>
                    @Html.ActionLink("Nauczyciel Prowadzący", "SelectTeacherBySubject", new { id = item.Id }) |
                    @Html.ActionLink("Edytuj", "SubjectEdit", new { id = item.Id }) |
                    @Html.ActionLink("Usuń", "DeleteSubject", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>
